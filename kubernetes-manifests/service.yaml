apiVersion: v1
kind: Service
metadata:
  name: demok8s-service
spec:
  selector:
    app: demok8s
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173
      nodePort: 30080
  type: NodePort
