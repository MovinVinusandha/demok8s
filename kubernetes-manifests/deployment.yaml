apiVersion: apps/v1
kind: Deployment
metadata:
  name: demok8s-deployment
  labels:
    app: demok8s
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: demok8s
    spec:
      containers:
      - name: demok8s-container
        image: movinvinusandha/testk8s:v3.1.0
        ports:
        - containerPort: 5173
        envFrom:
          - configMapRef:
              name: demok8s-configmap
  selector:
    matchLabels:
      app: demok8s
